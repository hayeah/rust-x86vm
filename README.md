A project to learn Rust and x86.

Build example programs:

```
make
```

run example program:

```
cargo run -- build/00.exit
```

* Basic MachO parsing support.
* Can only run `examples/00.exit.s` :p